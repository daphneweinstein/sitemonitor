usage: node app <alert factor>

where alert factor is the factor by which the last 4 load times must be larger than the average over the past day to generate an alert. this parameter is optional and if not set defaults to 2.