usage: node app <alert factor> <warning factor>

where alert factor is the factor by which the last 4 load times must be larger than the average over the past day to generate an alert. this parameter is optional and if not set defaults to 2.5, similarly, warning factor defaults to 1.5.